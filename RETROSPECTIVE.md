# ledger-lite 프로젝트 회고

- 기간: 2026-08-08 ~ 2026-08-17 (약 10일)
- 목적: 은퇴 후 생활비 소비를 촬영/입력만 하면 AI가 자동 분류·분석해주는 1인용 모바일 우선 AI 가계부
- 스택: Streamlit + Plotly, Claude API(Vision OCR·텍스트 분류), 카카오 로그인/알림, SQLite → Supabase(Postgres)
- 기술적 설계(스키마, 입력 채널, 대시보드 구조 등)는 [README.md](README.md)에 정리되어 있음. 이 문서는
  그 설계에 이르기까지 **무엇이 실패했고, 왜 그랬고, 어떻게 고쳤는지**를 남긴다.

## 1. 타임라인

| 날짜 | 마일스톤 |
|---|---|
| 08-08 | 1~6단계 MVP 전체 구축 (스키마 → 입력 채널 4종 → OCR/AI 분류 → 대시보드 → 카카오 알림), Streamlit Cloud 배포 |
| 08-09 ~ 08-10 | 실사용 피드백 기반 파서/카테고리 보정 (카드문자 형식 추가 지원, 카테고리 세분화, 버그 수정), 대시보드 1차 개선 |
| 08-10 ~ 08-16 | **(공백기) 데이터 유실 사고 발생 → 수기 백업 엑셀 재분류 → Supabase 마이그레이션 준비** |
| 08-16 | Supabase(Postgres) 듀얼 백엔드 전환, 백업 데이터 153건 일괄 재입력, 대시보드 전면 개편 시작 |
| 08-16 ~ 08-17 | 대시보드 UX 반복 개선 (사용자 피드백 라운드 4~5회, 매 라운드 브라우저 실검증 후 배포) |

## 2. 겪었던 문제와 원인

### 2-1. 데이터 유실 사고 (가장 큰 실패)

Streamlit Community Cloud는 앱을 컨테이너에서 실행하는데, `data/ledger.db`(로컬 SQLite 파일)는
그 컨테이너의 파일시스템에만 존재한다. README에는 배포 시점부터 "컨테이너 재시작/재배포 시
초기화될 수 있다"는 한계가 이미 적혀 있었지만, 실제로 재부팅이 일어나며 그동안 입력해둔
실거래 데이터가 전부 사라졌다.

**복구 방법**: 다행히 사용자가 별도로 관리하던 수기 백업 엑셀(`지출내역서_엑셀.xlsx`)이 있어,
그 안의 7~8월 거래 153건을 앱의 카테고리 규칙에 맞게 재분류(3라운드에 걸친 검수/수정)한 뒤
일회성 임포트 스크립트(`scripts/import_backup_excel.py`)로 일괄 재입력했다. 이 사고가 바로
Supabase 마이그레이션을 결정한 직접적 계기였다.

**교훈**: "로컬 모드로도 동작해야 한다"(API 키 없이 폴백)는 요구사항과 "데이터를 영구 보존해야
한다"는 요구사항은 완전히 다른 문제다. 무료 PaaS의 임시 파일시스템에 유일한 데이터 원본을 두면
안 되고, 이 위험은 배포 전에 반드시 우선순위를 매겨 처리했어야 했다.

### 2-2. Streamlit Cloud ImportError가 반복 발생 (가짜 알람)

배포된 앱이 `ImportError`를 내는데, `git show HEAD:<file>`로 확인하면 해당 이름이 커밋에
분명히 존재하는 상황이 두 번 있었다. 원인은 코드 결함이 아니라 **Streamlit Cloud의 모듈
캐시가 오래된 상태로 남아있는 것**으로 진단됐고, "Reboot app"으로 해결됐다.

- 1차(마이그레이션 전)에는 Reboot이 SQLite 데이터를 날릴 위험이 있어 신중하게 접근.
- 2차(Supabase 이전 후)에는 데이터가 외부 DB에 있으므로 안전하다고 판단하고 바로 Reboot.

**교훈**: 배포 환경 에러는 항상 "코드가 정말 틀렸는지"부터 커밋 히스토리로 확인하고, 캐시성
문제로 판단되면 재부팅 같은 무거운 조치도 안전성부터 따진 뒤 실행한다.

### 2-3. 완성된 코드를 커밋하지 않아 "배포엔 반영 안 됨" 상태로 방치

Postgres 듀얼 백엔드 코드가 로컬에서 완성/테스트까지 끝났는데 커밋을 깜빡해서, 배포된 앱은
여전히 예전 SQLite 코드로 돌고 있었다("데이터가 하나도 안 보인다" 리포트의 실제 원인).
`git status`로 미커밋 상태를 확인하고 나서야 원인이 밝혀졌다.

**교훈**: "로컬에서 됐다"와 "배포됐다"는 커밋·푸시라는 명시적 단계를 거쳐야 같아진다. 배포
이슈를 디버깅할 땐 로직보다 `git status`/`git log`부터 본다.

### 2-4. psycopg3(Postgres 드라이버) 전환 중 겪은 세부 함정

- `Cursor`가 `__slots__`를 써서 `cursor.lastrowid = ...`처럼 임의 속성을 못 붙임 →
  `_PGCursor` 래퍼 클래스로 우회.
- SQL에 들어있는 리터럴 `%`(나머지 연산자)가 psycopg의 `%s` 파라미터 치환과 충돌
  (`incomplete placeholder` 에러) → `%%`로 이스케이프.
- SQLite 전용 `INSERT OR IGNORE`는 Postgres에 없음 → 표준 `INSERT ... ON CONFLICT (...) DO
  NOTHING`으로 통일해서 SQLite/Postgres 양쪽에서 그대로 동작하게 만듦 (분기 자체를 제거).

**교훈**: 두 DB를 동시 지원할 땐 방언 분기를 늘리기보다, 애초에 양쪽이 다 지원하는 표준
문법을 찾는 쪽이 유지보수 비용이 훨씬 낮다.

### 2-5. 그 외 자잘한 버그들

- 은행이체 수동입력에서 대분류를 바꿔도 소분류 목록이 갱신되지 않던 UI 버그.
- 사용자가 엑셀의 "카테고리" 참조 시트를 드롭다운으로 직접 편집하다가, 앱 스키마와 맞지 않는
  카테고리 조합(예: 수입 대분류에 실제로 없는 값)이 섞여 들어감 → 정합성 검증 후 올바른 신규
  카테고리(`수입 > 모임정산`)를 추가하는 방식으로 해결.
- 백업 재분류 스크립트가 가맹점 이름만 보고 매칭하다가 동명이인의 수입 건과 지출 건을 둘 다
  잘못 덮어씀 → 검증 단계에서 발견, 금액/방향까지 함께 확인하도록 수정.

## 3. Supabase 적용 해결 방안

핵심 설계는 "서비스 레이어의 SQL을 한 줄도 바꾸지 않고" SQLite와 Postgres를 동시에 지원하는 것이었다.

1. **연결 계층 추상화** (`app/db/connection.py`): `SUPABASE_DB_URL` 환경변수 존재 여부로
   SQLite/Postgres를 분기. `_PGConnection`/`_PGCursor`가 `sqlite3.Connection`의 `?` 플레이스홀더,
   dict 형태 row 접근, `.lastrowid` API를 그대로 흉내 내서 상위 서비스 코드가 백엔드를 몰라도
   되게 만들었다.
2. **스키마 이원화** (`app/db/schema_postgres.sql`): `AUTOINCREMENT` ↔
   `GENERATED ALWAYS AS IDENTITY`, `strftime` ↔ `to_char(... AT TIME ZONE 'Asia/Seoul', ...)`,
   `PRAGMA` 없음, `updated_at` 트리거를 plpgsql 함수로 재구현.
3. **SQL 조각 추상화** (`app/db/dialect.py`): 연/월, 주 시작일 등 백엔드마다 문법이 다른 표현을
   함수로 감싸고, 호출 시점에 `SUPABASE_DB_URL`을 확인해 분기(테스트에서 monkeypatch 가능하도록
   import 시점이 아니라 호출 시점에 체크).
4. **포터블 SQL 우선**: 방언 분기 대신 SQLite 3.24+/Postgres 양쪽이 지원하는 표준 문법(예:
   `ON CONFLICT DO NOTHING`)을 최대한 사용해 분기 자체를 줄임.
5. **검증**: 91개 로컬 테스트(SQLite 경로)로 회귀 방지 + 실제 Supabase 인스턴스에 대한
   스모크 테스트(임포트 스크립트 실행, 브라우저로 대시보드 수치 대조)로 프로덕션 경로까지 확인.

결과적으로 데이터는 이제 앱 컨테이너가 아니라 Supabase(관리형 Postgres)에 저장되어, Streamlit
Cloud가 재시작/재배포되어도 유실되지 않는다.

## 4. 대시보드 UX 반복 개선

Supabase 전환 이후 사용자가 실제 데이터로 대시보드를 쓰면서 4~5라운드에 걸쳐 피드백을 줬고,
매 라운드 로컬에서 브라우저로 직접 클릭해 시각적으로 검증한 뒤에만 배포했다.

- 페이지 개편 → 사용자가 기존 이름/레이아웃을 더 선호해 되돌림 (새로운 게 항상 낫진 않다).
- "포함 제외 토글이 안 보인다"는 반복 리포트 → 근본 원인은 버그가 아니라 **레이아웃상 필터와
  그 적용 대상이 멀리 떨어져 있어서 발생한 발견성(discoverability) 문제**였다. 섹션 순서를
  재배치하고 안내 문구를 상시 노출해서 해결.
- 드롭다운 멀티셀렉트 → 가로 토글 버튼(`st.pills`)으로 교체해 클릭 한 번으로 상태가 바로
  보이게 함.
- 도넛/TOP10/카테고리별 상세가 각자 다른 방식으로 기간을 정해 헷갈리던 것을, 화살표 하나로
  세 영역이 같은 달을 함께 이동하도록 통합.

**교훈**: "버그인 줄 알았던 것"이 실제로는 UI 배치 문제인 경우가 많다 — 코드를 먼저 의심하기보다
사용자가 화면에서 실제로 무엇을 보고 있는지(스크린샷)부터 확인한다.

## 5. Key Learnings (다음 프로젝트에 재사용할 것)

1. **무료 PaaS의 로컬 파일시스템은 "임시"로 취급한다.** 실사용 데이터가 걸린 순간 관리형 DB
   전환을 뒤로 미루지 않는다 — 이번엔 사고가 나고서야 움직였다.
2. **배포 이슈는 코드보다 배포 상태부터 의심한다.** `git status`(미커밋 여부), `git log`(실제
   반영 여부), 그다음에야 로직.
3. **정체불명의 배포 환경 에러는 캐시 문제일 수 있다.** 재부팅 같은 무거운 조치는 그로 인한
   부작용(예: 휘발성 데이터 손실)이 없는지부터 먼저 확인한 뒤 실행한다.
4. **듀얼 DB 지원은 방언 분기보다 표준 SQL 채택이 먼저다.** 분기를 늘리는 대신 양쪽이 다
   지원하는 문법을 찾으면 유지보수 부담이 크게 준다.
5. **"버그 리포트"를 코드 문제로 단정하지 않는다.** 스크린샷/화면 배치부터 확인하면 실제로는
   발견성·레이아웃 문제인 경우가 많다.
6. **민감한 개인 데이터 파일은 시작부터 `.gitignore`.** 백업 엑셀처럼 실제 금액이 담긴 파일은
   프로젝트 초반에 바로 제외 규칙을 걸어둔다.
7. **UI 반복은 매 라운드 실제 화면으로 검증한다.** 코드가 맞아 보여도 브라우저로 클릭해보기
   전엔 "적용 안 됐다"는 리포트가 반복될 수 있다.

## 6. 만약 이어간다면 (향후 개선 아이디어)

- Supabase 데이터의 정기 백업(예: 주기적 CSV export)을 만들어 "두 번째 사고"에 대비.
- 카카오 로그인/알림처럼 실제 계정 인증이 필요해 자동화 테스트로 못 덮은 경로를 주기적으로
  수동 점검하는 체크리스트화.
- 카테고리 참조 시트 같은 "사용자가 직접 편집 가능한 소스"와 앱 스키마 간 정합성을 사전에
  검증하는 가벼운 스크립트.
